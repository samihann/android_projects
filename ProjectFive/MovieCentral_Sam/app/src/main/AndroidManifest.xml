<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samihann.MovieCentral">

    <!--    By Samihan Nandedkar   -->
    <!--    UIN: 667142409         -->
    <!--    CS 478 Spring-22       -->
    <!--    Project Five : Movie Central Application  -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCentral_Sam">
        <service
            android:name=".MovieCentral"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.samihann.MovieCentral" />
            </intent-filter>
        </service>
    </application>

</manifest>